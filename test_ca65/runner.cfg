# ld65 configuration for the ca65 test runner: a plain X16 PRG.
# LOADADDR carries the 2-byte load address, CODE holds everything else
# (this port is a single translation unit, code and data interleaved,
# exactly like the ACME build).

MEMORY {
    HDR:  start = $07FF, size = $0002, file = %O;
    MAIN: start = $0801, size = $9700, file = %O;
}

SEGMENTS {
    LOADADDR: load = HDR,  type = ro;
    CODE:     load = MAIN, type = rw;
}
